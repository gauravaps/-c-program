#include<stdio.h>
#include<math.h>
int main()
{
    int a,b,c;
    printf("enter the value a:");
    scanf("%d",&a);
    printf("enter the value b:=");
    scanf("%d",&b);
    c=pow(a,b);
    printf("power of a:= %d" ,c);
    return 0;
}
